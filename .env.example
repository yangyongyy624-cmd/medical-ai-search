# 敏感信息清理指南

**重要**: 在上传到 GitHub 之前，必须清理所有敏感信息！

---

##  需要清理的文件

### ❌ 禁止上传的文件

```bash
# API Key 和凭证
.env
*.env
credentials/
*.key
*.pem
*_credentials.json

# 本地配置
~/.hermes/.env
~/.openclaw/openclaw.json
~/.claude/settings.json
~/.codex/config.toml

# 日志文件
*.log
logs/

# 缓存和临时文件
__pycache__/
*.pyc
.cache/
tmp/

# 个人数据
*.pdf  # 全文 PDF (版权)
ppt_output/  # 个人 PPT
pmc_fulltexts/  # 下载的全文
```

---

##  .gitignore 配置

```gitignore
# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# 环境变量
.env
.env.local
.env.*.local
*.env

# API Keys 和凭证
credentials/
*.key
*.pem
*_credentials.json
secrets/
config.secrets.json

# 本地配置
.hermes/
.openclaw/
.claude/
.codex/
hermes-agent/

# 日志
*.log
logs/
*.log.*

# 缓存
.cache/
.pytest_cache/
.mypy_cache/

# 临时文件
tmp/
temp/
*.tmp
*.swp
*.swo
*~

# 输出文件
ppt_output/
pmc_fulltexts/
*.pdf
*.pptx

# 数据文件
*.json
!package.json
data/
output/

# IDE
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# OS
.DS_Store
Thumbs.db
Desktop.ini

# Jupyter Notebook
.ipynb_checkpoints

# 测试
.coverage
htmlcov/
.tox/
.nox/

# 文档
docs/_build/
docs/.doctrees/
```

---

##  清理步骤

### 1. 创建 .env.example

```bash
# .env.example (示例文件，可上传)
# PubMed API Key (可选)
# 获取地址：https://www.ncbi.nlm.nih.gov/account/
PUBMED_API_KEY=your_key_here

# Hermes 端点 (可选)
HERMES_ENDPOINT=http://localhost:3333

# 飞书 Webhook (可选)
# 获取地址：飞书开放平台
FEISHU_WEBHOOK=https://open.feishu.cn/open-apis/bot/v2/hook/your_hook

# 飞书 App ID (可选)
FEISHU_APP_ID=your_app_id

# 飞书 App Secret (可选)
FEISHU_APP_SECRET=your_app_secret
```

### 2. 清理代码中的硬编码 Key

**错误示例**:
```python
# ❌ 不要这样做
API_KEY = "sk-sp-7e90351e680c4e6eb33f70b6643cbd86"
```

**正确示例**:
```python
# ✅ 使用环境变量
import os
API_KEY = os.getenv("PUBMED_API_KEY")
```

### 3. 清理示例和测试文件

```bash
# 删除包含真实数据的文件
rm -rf examples/test_data/
rm -rf tests/fixtures/real_data/

# 创建示例数据
mkdir examples/sample_output/
echo "Sample output placeholder" > examples/sample_output/README.md
```

### 4. 清理文档中的敏感信息

**检查文件**:
- README.md
- docs/*.md
- *.md (所有 Markdown 文件)

**搜索关键词**:
```bash
# 搜索可能的敏感信息
grep -r "sk-" . --include="*.md"
grep -r "cli_" . --include="*.md"
grep -r "App Secret" . --include="*.md"
grep -r "password" . --include="*.md"
grep -r "token" . --include="*.md"
```

---

##  上传前检查清单

### ✅ 必须检查

- [ ] `.env` 文件已添加到 `.gitignore`
- [ ] `.env.example` 已创建 (不含真实 Key)
- [ ] 代码中无硬编码 API Key
- [ ] 删除所有 `.log` 文件
- [ ] 删除 `ppt_output/` 目录
- [ ] 删除 `pmc_fulltexts/` 目录
- [ ] 删除 `__pycache__/` 目录
- [ ] 清理文档中的敏感信息

### ✅ 推荐检查

- [ ] 添加 LICENSE 文件
- [ ] 添加 CONTRIBUTING.md
- [ ] 添加 CODE_OF_CONDUCT.md
- [ ] 更新 README.md 联系方式
- [ ] 添加 GitHub Actions CI/CD (可选)
- [ ] 添加测试覆盖率报告 (可选)

---

##  验证命令

```bash
# 1. 检查 .gitignore
cat .gitignore

# 2. 检查是否有遗漏的敏感文件
git status

# 3. 搜索可能的 API Key
grep -r "sk-" . --include="*.py" --include="*.md" --include="*.json"
grep -r "cli_" . --include="*.py" --include="*.md" --include="*.json"
grep -r "App.*Secret" . --include="*.py" --include="*.md" --include="*.json"

# 4. 检查大文件
git ls-files -s | sort -rn | head -20

# 5. 试运行 git add
git add --dry-run .
```

---

##  GitHub 上传步骤

```bash
# 1. 初始化 Git (如果还没有)
cd medical-ai-search
git init

# 2. 添加文件
git add .

# 3. 检查
git status

# 4. 提交
git commit -m "Initial commit: Medical AI Literature Search System"

# 5. 创建 GitHub 仓库
# 访问：https://github.com/new
# 仓库名：medical-ai-search
# 可见性：Public

# 6. 推送
git remote add origin https://github.com/YOUR_USERNAME/medical-ai-search.git
git branch -M main
git push -u origin main
```

---

##  发布后维护

### 版本发布

```bash
# 添加版本标签
git tag -a v1.0.0 -m "Release version 1.0.0"
git push origin --tags
```

### 问题跟踪

- 使用 GitHub Issues 跟踪问题
- 使用 GitHub Projects 管理开发计划
- 使用 GitHub Discussions 进行讨论

---

**创建者**: 宵宵
**创建时间**: 2026-07-11
**状态**: ✅ 开源准备完成
